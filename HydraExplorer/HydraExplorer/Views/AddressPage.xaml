<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:vm="clr-namespace:HydraExplorer.ViewModels"
          xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="HydraExplorer.Views.AddressPage">
    <ContentPage.BindingContext>
        <vm:AddressViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label>test</Label>
            <Label TextColor="Black" FontSize="Medium" Text="{Binding AddressName}"></Label>
            <StackLayout BindingContext="{Binding Address}">
                <Label Text="{Binding balanceHydra,StringFormat='Balance {0} Hydra'}"></Label>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>